language: python

python:
  - "3.6"

cache: pip

addons:
  chrome: stable

notifications:
  email: false

install: 
  - make chromedriver
  - make pipenv

jobs:
  include:
    - script: pytest -m eight88sport --proxy $PROXY
    - script: pytest -m bet365
    - script: pytest -m bwin
    - script: pytest -m williamhill
    - stage: coverage
      script: make coverage
